# ====================================
# Nginx Web 应用配置文件
# ====================================

# ====================================
# 基本配置
# ====================================

# 容器名称（必填）
# 用于标识 Docker 容器，确保唯一性
CONTAINER_NAME=nginx_web_app

# 端口映射（必填）
# 格式：宿主机端口:容器端口
# 例如：8080:80 表示将容器的 80 端口映射到宿主机的 8080 端口
PORT=8080:80

# 网站根目录（必填）
# 你的静态网站文件所在的本地路径
# 该目录会被挂载到容器的 /data 目录
WEB_ROOT=/data/www

# ====================================
# 高级配置（可选）
# ====================================

# Nginx 镜像版本
# 默认：latest
# 可选值：latest, stable, 1.25, 1.24, alpine 等
NGINX_VERSION=latest

# 自定义 Docker 镜像（可选）
# 如果你构建了自己的镜像，可以在此指定
# 设置后将忽略 NGINX_VERSION
# CUSTOM_IMAGE=my-nginx:1.0

# ====================================
# 日志配置（可选）
# ====================================

# 日志文件最大大小
# 默认：10m
# 当日志文件达到此大小时会自动轮转
LOG_MAX_SIZE=10m

# 保留的日志文件数量
# 默认：3
# 系统会保留最近的 N 个日志文件
LOG_MAX_FILE=3
